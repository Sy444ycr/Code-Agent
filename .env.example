# 示例环境配置文件。
# 密钥优先保存到系统 keyring（code-agent auth set <name>）。
# 以下仅供显式允许开发回退的本地调用：值为明文且对进程可见，不要填入或提交真实凭据。

CODE_AGENT_PROVIDER_OPENAI_API_KEY=

# 真实 Provider E2E 默认关闭。设为 1 才会运行；名称只选择非敏感 Provider 档案，不能存放密钥。
CODE_AGENT_RUN_PROVIDER_E2E=
CODE_AGENT_PROVIDER_E2E_NAME=openai
