# 反思报告

## AI 辅助说明

本文基于本项目的 `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md`、`AGENT_LOG.md`、提交历史和验证记录生成初稿，并经 AI 辅助整理、润色。文中关于项目实现、测试和过程证据的表述均以仓库已有记录为依据；最终提交前由本人复核其与实际开发经历的一致性。

## 从“让模型写代码”到“对工程结果负责”

本项目的目标是实现一个 Coding Agent Harness。它不是把某个现成大模型接口包上一层命令行，而是要求自己实现任务主循环、工具分发、治理护栏、反馈闭环、记忆、配置和多种交互入口，并能够在移除真实 LLM 后仍用 Mock LLM 进行确定性验证。完成这个项目后，我对 AI4SE 最深的体会是：当 AI 已经可以很快地产出代码时，人的价值并没有消失，而是从“输入代码”转向界定问题、建立约束、判断证据与承担最终责任。

一开始，我倾向于把功能列表直接交给智能体，希望尽快得到一个可运行的原型。实践中很快发现，这种方式只会把不确定性向后推：接口字段没有定义清楚、危险操作的边界模糊、恢复语义没有写明，最终都会在测试或集成阶段变成返工。后来，项目先在 `SPEC.md` 中明确问题、用户故事、状态机、数据模型、安全边界和验收标准，再把这些内容拆为 `PLAN.md` 中可验证的任务。这个变化看似增加了文档工作，实际降低了沟通成本。智能体不再需要猜测“完成”意味着什么，评审也能够针对规格和测试提出具体意见。

## Superpowers 方法的价值与限制

在本项目中，`brainstorming`、`writing-plans`、`using-git-worktrees`、`test-driven-development`、`requesting-code-review` 和 `verification-before-completion` 构成了相对完整的开发节奏。其中最有帮助的是把模糊需求外化为规格和计划的两个阶段。尤其是对任务服务、REST/SSE 生命周期、Provider 抽象、WebUI 打包和 ECS 部署等跨度较大的工作，先写清输入、状态转换、失败路径和验证命令，比直接开始编码可靠得多。

`SPEC_PROCESS.md` 的作用也超出了一份过程日志。它记录了关键设计选择，例如默认使用离线 Mock Provider、真实 Provider 必须显式配置、重启后审批任务需要进入可恢复状态、首次公网演示采用 IP/HTTP 而不把它描述为生产级部署。这些记录使后来回看实现时能区分“有意不做”与“遗漏未做”，也让我能够解释每一项边界背后的原因。

不过，Superpowers 并不是自动保证质量的魔法流程。流程如果只留下技能名称而没有红绿测试、审查结论和人工决策，就会变成形式上的合规。项目中一些任务的设计文档、实施计划和日志较为完整，但这并不等于每一次智能体输出都天然正确。特别是在多线程任务恢复、SSE 游标回放和打包资产处理这类问题上，真正有价值的是把错误重现为稳定测试，再用最小改动修正，而不是在日志中简单写“已完成”。我认为流程的核心价值在于强迫开发者面对证据，而不是让开发者拥有更多文档。

## TDD、评审与任务粒度

本项目采用 Red-Green-Refactor 的方式推进核心功能。TDD 对 AI 协作并不是阻碍，反而是防止“看起来合理但无法验证”的输出扩散的放大器。比如治理策略、Mock 决策循环、反馈闭环、恢复审批和容器持久化都需要确定性测试。测试先失败时，能够证明需求尚未被现有代码满足；最小实现后同一测试转绿，才说明改动确实解决了目标问题。这样做也限制了智能体一次修改过多文件的倾向，使问题和修复之间的对应关系更加清楚。

TDD 也有成本。对于 WebUI 视觉细节、真实云服务器操作或依赖外部网络的 Provider E2E，无法只靠普通单元测试覆盖。因此本项目把离线 Mock 路径作为默认验收，把真实 Provider 和 Docker 运行时验证设为显式开关，并在日志中区分“已通过”“未执行”和“环境限制”。这种区分比把所有检查笼统写成成功更诚实，也更有助于答辩时解释风险。

subagent 与 worktree 的经验同样重要。独立任务在隔离 worktree 中进行，能减少并发修改相互覆盖的风险，也使每个功能的提交和审查范围更清晰。但任务拆分并非越细越好：如果一个任务没有完整的输入、输出和验证方式，交给新的 subagent 后仍会依赖隐含上下文，反而增加沟通成本。较合适的粒度应当让一个 agent 能在一次会话中完成一个可测试的闭环，例如“增加某条 API 生命周期语义及其集成测试”，而不是只做一个没有独立验收意义的小函数。

## 安全、分发与交付的反思

本项目让我认识到，凭据安全和分发不是开发结束后的附加项。设计中明确将 API Key 放在系统 keyring，并让 `.env` 仅作为被忽略的本地输入来源；配置文件不保存密钥，Shell 执行时移除 Provider 密钥环境变量。这个方案并不能消除全部风险，但至少把“密钥会不会进入仓库、日志、终端历史或子进程环境”变成了可以检查的工程问题。

分发方面，项目补充了 Dockerfile、Docker Compose、Python 包打包、前端静态资源准备、GitHub Actions 和 GitLab `unit-test` job。实际执行中，容器构建、前端产物和 wheel/sdist 的关系比预期复杂：只有把前端构建、资源暂存、Python 校验、打包和干净安装放到同一发布链中，才能避免“开发环境可运行、安装后的包缺资源”的问题。ECS 部署文档则将服务器重装、安全组、Docker Compose、SQLite 持久卷、升级和回滚步骤明确下来。公共 WebUI 的可访问性、最后一次 CI 成功和文档准确性，都是交付物的一部分，而不是代码通过测试后可以忽略的细节。

## 结语

回顾整个项目，我学到的不是如何让 AI 生成更多代码，而是如何让 AI 生成的结果进入可追溯、可审查、可复现的工程闭环。规格为智能体提供边界，计划把边界转化为可执行任务，测试提供客观反馈，评审和日志保留人工判断，分发与部署则检验系统是否真的能被他人使用。未来如果继续迭代，我会进一步加强独立评审、真实 Provider 的隔离 E2E、TLS 与访问控制、备份和监控；但这些增强同样必须建立在清晰的需求、明确的风险边界和可验证的证据之上。
